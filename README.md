# color-object-tracking
This project detects and tracks objects based on their HSV color ranges using OpenCV. It provides a Tkinter-based GUI for parameter adjustment, supports CSV logging, snapshot and video recording, and generates graphs for object area and speed analysis.
